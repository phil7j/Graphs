
from util import Stack, Queue

def earliest_ancestor(ancestors, starting_node):
    pass
# Traverse depth first, maybe??

# Store each path in an array

# All paths are in arrays stored in an array of arrays

# After traversing through graph, find the path that has the longest length

# Then return the last item in that path

# If there are two or more path's with the same length,
# compare the last item of all of them and return the smallest